{
  "homepage": "http://{username}.github.io/{repo-name}",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build":"npm run dev"
    },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
